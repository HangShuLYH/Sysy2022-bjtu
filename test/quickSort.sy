const int TOKEN_NUM = 0, TOKEN_OTHER = 1;
int last_char = 32, num, other;
int cur_token;

int next_char() {
    return 0;
}

int is_space(int c) {
    return 0;
}
int next_token() {
  while (is_space(last_char)) next_char();
  return cur_token;
}

int main() {
  return 0;
}